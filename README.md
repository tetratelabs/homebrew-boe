# Homebrew Tap for Built On Envoy

This is the Homebrew formula to install the [Built On Envoy](https://builtonenvoy.io/) CLI.

Install the `boe` CLI as follows:

```shell
brew tap tetratelabs/homebrew-boe
brew install boe
```
